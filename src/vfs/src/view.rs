/*
use crate::{Vfs, VfsFile};
use std::{collections::HashMap, path::PathBuf};

pub struct VfsView<'a> {
    vfs: &'a Vfs,
    buffers: HashMap<PathBuf, VfsFile>,
}

pub struct VfsViewMut<'a> {
    vfs: &'a mut Vfs,
    buffers: HashMap<PathBuf, VfsFile>,
}

*/
